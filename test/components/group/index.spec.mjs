import {
  expect
} from 'chai'

import Group from '#sprockets/components/group'

describe('#sprockets/components/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
