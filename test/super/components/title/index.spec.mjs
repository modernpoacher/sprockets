import {
  expect
} from 'chai'

import Title from '#sprockets/super/components/title'

describe('#sprockets/super/components/title', () => {
  describe('`Title`', () => {
    it('is a function', () => {
      expect(Title)
        .to.be.a('function')
    })
  })
})
