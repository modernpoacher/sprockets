import {
  expect
} from 'chai'

import CheckAnswers from '#sprockets/sprockets/check-answers'

describe('#sprockets/sprockets/check-answers', () => {
  describe('`CheckAnswers`', () => {
    it('is a function', () => {
      expect(CheckAnswers)
        .to.be.a('function')
    })
  })
})
