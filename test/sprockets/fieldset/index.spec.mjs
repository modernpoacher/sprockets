import {
  expect
} from 'chai'

import Fieldset from '#sprockets/sprockets/fieldset'

describe('#sprockets/sprockets/fieldset', () => {
  describe('`Fieldset`', () => {
    it('is a function', () => {
      expect(Fieldset)
        .to.be.a('function')
    })
  })
})
