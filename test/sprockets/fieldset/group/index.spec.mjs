import {
  expect
} from 'chai'

import Group from '#sprockets/sprockets/fieldset/group'

describe('#sprockets/sprockets/fieldset/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
