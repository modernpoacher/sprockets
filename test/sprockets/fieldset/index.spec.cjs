const {
  expect
} = require('chai')

const Fieldset = require('#sprockets/sprockets/fieldset')

describe('#sprockets/sprockets/fieldset', () => {
  describe('`Fieldset`', () => {
    it('is a function', () => {
      expect(Fieldset)
        .to.be.a('function')
    })
  })
})
