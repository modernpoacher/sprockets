const {
  expect
} = require('chai')

const Group = require('#sprockets/sprockets/error-summary/group')

describe('#sprockets/sprockets/error-summary/group', () => {
  describe('`Group`', () => {
    it('is a function', () => {
      expect(Group)
        .to.be.a('function')
    })
  })
})
