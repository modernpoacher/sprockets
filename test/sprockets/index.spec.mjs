import {
  expect
} from 'chai'

import Sprocket from '#sprockets/sprockets'

describe('#sprockets/sprockets', () => {
  describe('`Sprocket`', () => {
    it('is a function', () => {
      expect(Sprocket)
        .to.be.a('function')
    })
  })
})
